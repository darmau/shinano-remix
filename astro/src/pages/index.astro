---
// Placeholder root index. The middleware (src/middleware/index.ts) negotiates
// Accept-Language and 307s to /zh|/en|/jp before this ever renders; this file
// only exists so Astro's i18n routing validator is happy.
return Astro.redirect("/zh");
---
