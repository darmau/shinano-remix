{
  "name": "shinano",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter shinano-astro dev",
    "build": "pnpm --filter shinano-astro build",
    "check": "pnpm --filter shinano-astro check",
    "preview": "pnpm --filter shinano-astro preview",
    "deploy": "pnpm --filter shinano-astro exec wrangler deploy",
    "typegen": "pnpm --filter shinano-astro typegen"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.17.0"
}
